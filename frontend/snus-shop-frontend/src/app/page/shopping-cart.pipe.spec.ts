import { ShoppingCartPipe } from './shopping-cart.pipe';

describe('ShoppingCartPipe', () => {
  it('create an instance', () => {
    const pipe = new ShoppingCartPipe();
    expect(pipe).toBeTruthy();
  });
});
